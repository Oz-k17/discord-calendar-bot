/**
 * 試し用素材の「正解」。
 *
 * どこでしゃべっているかを作る側と測る側の両方から参照できるようにしておく。
 * 正解が分かっていれば、「この特徴量は声とそれ以外をどれくらい分けられるか」を
 * 当てずっぽうではなく数字で比べられる。
 */

export const SHORT_LENGTH = 13;

/**
 * まばらな発話（秒）。BGM の上でたまにしゃべるだけ、という素材のため。
 * 13 秒のうち 2.6 秒しかしゃべらない（20%）。
 */
export const SPARSE_UTTERANCES = [
  [2.0, 3.4],
  [9.0, 10.2],
];

/** 発話の並び（秒）。息継ぎ（0.2 秒）と間（0.7〜1.2 秒）を混ぜてある。 */
export const UTTERANCES = [
  [1.0, 2.2],
  [2.9, 4.4],
  [4.6, 5.4],
  [6.6, 8.0],
  [8.2, 8.9],
  [10.0, 11.6],
];

/**
 * 短いほうの素材の一覧。`hard` は「声を見分ける処理をいじめるために足したもの」。
 * options はそのまま make-audio.mjs の makeShort へ渡る。
 */
export const SHORT_FIXTURES = [
  { name: 'speech.wav', note: 'きれいに録れた声', speech: true, options: { seed: 1 } },
  { name: 'speech-bgm.wav', note: 'BGM 込みで録った', speech: true, options: { bgm: true, seed: 2 } },
  { name: 'speech-noisy.wav', note: '部屋のノイズが大きい', speech: true, options: { noiseLevel: 0.02, seed: 3 } },
  {
    name: 'speech-quiet.wav',
    note: '録音レベルが小さい',
    speech: true,
    options: { speechLevel: 0.06, noiseLevel: 0.0006, seed: 4 },
  },
  { name: 'bgm.wav', note: '音楽だけ', speech: false, options: { speech: false, bgm: true, noiseLevel: 0.0005, seed: 5 } },

  // ここから下は意地悪な素材。「うまくいった」が素材のおかげでないことを確かめるためのもの。
  {
    name: 'speech-bgm-loud.wav',
    note: 'BGM が大きい（音量では分けられない）',
    speech: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.4, seed: 6 },
  },
  {
    name: 'speech-drums.wav',
    note: '1 秒に 4 回刻む打楽器の上でしゃべる',
    speech: true,
    hard: true,
    options: { beat: 4, seed: 7 },
  },
  {
    name: 'drums.wav',
    note: 'その打楽器だけ（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, beat: 4, noiseLevel: 0.0005, seed: 8 },
  },
  {
    name: 'music-tremolo.wav',
    note: '声と同じ速さで震える楽器だけ（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, bgm: true, bgmLevel: 0.3, bgmTremolo: 4.5, noiseLevel: 0.0005, seed: 10 },
  },
  {
    // 「包絡（スペクトルの形）が動いたか」で声を見分ける手を潰しにいく素材。
    // 音量だけでなく**形そのもの**が声と同じ速さで動くので、
    // トレモロを弾けた手（shapeChange）でも弾けないはず。
    name: 'music-wah.wav',
    note: '声と同じ速さでフォルマントが動く楽器（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, wah: 4.2, noiseLevel: 0.0005, seed: 13 },
  },
  {
    // 「どこかで形が動いたら声がある」とみなす素材単位の判定を潰しにいく素材。
    // 本物の曲はたいてい和音が変わるので、鳴りっぱなしの bgm.wav よりこちらが普通。
    name: 'music-chords.wav',
    note: '和音が 1.5 秒ごとに変わる音楽（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, chordEvery: 1.5, noiseLevel: 0.0005, seed: 14 },
  },
  {
    // **包絡の門＋保持**（2026-09-11・3 回目）を潰しにいく素材。
    // 保持は「いったん音色が動いたら 0.5 秒は開けたまま」なので、
    // **保持より短い間隔で音色が動き続ける音楽**なら、門は一度も閉まらない。
    // 和音を 0.4 秒ごとに変えれば、声がゼロでも門を開けっぱなしにできるはず。
    // 現実の曲でも、刻みの速い伴奏はこれくらいの速さで音が変わる。
    name: 'music-chords-fast.wav',
    note: '和音が 0.4 秒ごとに変わる音楽（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, chordEvery: 0.4, noiseLevel: 0.0005, seed: 16 },
  },
  {
    // **素材単位の形の判定そのもの**を潰しにいく素材（2026-09-12）。
    // 0.4 秒ごとでは「形が動いた秒数」が 0.30 秒までしか伸びず、しきい値 0.5 秒に届かなかった。
    // ところがこれは「声が無いから」ではなく、**変化の間隔が均す窓（0.15 秒）より広いから**
    // 棘が均されて消えていただけ。間隔を窓より狭くすれば、声がゼロでも埋め尽くせるはず。
    // 0.2 秒ごとは 16 分音符（BPM 150）くらいで、刻みの速い伴奏なら現実にいくらでもある。
    name: 'music-chords-faster.wav',
    note: '和音が 0.2 秒ごとに変わる音楽（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, chordEvery: 0.2, noiseLevel: 0.0005, seed: 17 },
  },
  {
    // 「形の変化」で判定する手を潰しにいく素材。背景がほとんど無いので、
    // 声が背景と混ざる効果に頼っていると、ここで声を見失う。
    name: 'speech-dry.wav',
    note: '背景がほぼ無い、乾いた声だけ',
    speech: true,
    hard: true,
    options: { noiseLevel: 0.00002, seed: 11 },
  },
  {
    // 「包絡が動いていないコマは声ではない」とコマ単位で判断する手を潰しにいく素材。
    // 母音を伸ばしている間は口が動かないので、包絡も動かない。
    // **伸ばした母音は現実の話し声に普通にある**ので、ここで声を切るのは
    // 素材の欠陥ではなく判定の欠陥になる。背景も無いので逃げ場は無い。
    name: 'speech-sustained.wav',
    note: '乾いた声・母音を長く伸ばす',
    speech: true,
    hard: true,
    options: { noiseLevel: 0.00002, sustain: true, seed: 12 },
  },
  {
    // **子音に頼る判定を潰しにいく素材**（2026-09-12・2 回目）。
    // この日に `speak()` へ子音と息を足したので、今後「広い帯域に散る雑音の粒があるか」で
    // 声を見分ける手が考えられる。ところが**子音がほとんど無い発声は現実にある**
    // （ハミング・詠唱・歌の母音・伸ばした「あー」）。子音を必須にすると、そこで声を切る。
    // `speech-dry.wav` と**子音と息の有無だけ**が違う（背景も乱数の種も同じ 11）ので、
    // 2 本を並べれば「その判定は子音を見ているのか、声を見ているのか」が出る。
    name: 'speech-vowels-only.wav',
    note: '乾いた声・子音も息も無い（ハミングのような発声）',
    speech: true,
    hard: true,
    options: { noiseLevel: 0.00002, vowelsOnly: true, seed: 11 },
  },
  {
    // **重心の下降率を「コマ単位」で使う手**を潰しにいく素材（2026-09-13・2 回目）。
    //
    // その手は `speech-hats.wav`（子音も息もある声）では潰れなかった（声のコマの 29% だけが
    // 打点の側に落ちる）。ところがそれは、**声の子音と息がハイハットの帯域を覆っていた**から。
    // 重心はいちばん高い所にある音に引きずられるので、声が高い帯域に何も出していなければ、
    // 重心を動かしているのはハイハットの減衰だけになる。
    // 子音の無い声なら、**声が鳴っているコマまで打点に見える**はず（実測 83%）。
    //
    // `speech-vowels-only.wav` と**ハイハットの有無だけ**が違う（声は 1 ビットも同じ）。
    // ハミング・詠唱・歌の母音を伴奏の上でやるのは現実にいくらでもあるので、
    // これを切るのは判定の欠陥になる。
    name: 'speech-vowels-hats.wav',
    note: 'ハイハットの上でハミングする（子音も息も無い声）',
    speech: true,
    hard: true,
    options: { noiseLevel: 0.00002, vowelsOnly: true, hat: 4.2, hatLevel: 0.1, seed: 11 },
  },
  {
    // **「高い帯域だけが動いているコマは打点だ」で打点を弾く手**を潰しにいく素材
    // （2026-09-13・3 回目。**この素材がその手を潰したので、判定には入れていない**）。
    //
    // その手が見ているのは「低い帯域が動かないまま高い帯域だけが動いたか」。
    // **母音を伸ばしている間は低い帯域が動かない**（`speech-sustained.wav` が
    // 包絡の門を潰したのと同じ性質）ので、その上でハイハットが刻むと、
    // 声が鳴っているコマまで「高い帯域だけが動いた」に見えるはず。
    // ハミング（`speech-vowels-hats.wav`）が高い帯域を覆えないのとは別の破り方で、
    // こちらは**低い側が止まる**ほうから攻める。実際そのとおりになり、
    // 声のコマの中央値 0.571・84% が 0.25 超えで、`music-hats`（0.571・99%）とほぼ重なった。
    // 門を入れると声を残せた率が 100% → 73% に落ちる。詳しくは JOURNAL の同日。
    //
    // `speech-sustained.wav` と**ハイハットの有無だけ**が違う（声は 1 ビットも同じ）。
    // 伸ばした母音は現実の話し声に普通にあり、その裏で伴奏が刻むのも普通なので、
    // ここで声を切るのは判定の欠陥になる。
    name: 'speech-sustained-hats.wav',
    note: 'ハイハットの上で母音を長く伸ばす声',
    speech: true,
    hard: true,
    options: { noiseLevel: 0.00002, sustain: true, hat: 4.2, hatLevel: 0.1, seed: 12 },
  },
  {
    // 「音量の揺れが規則正しすぎるものは楽器だ」で音楽を弾く手を潰しにいく素材。
    // 音節の長さが揃っているだけで、中身はまぎれもない声（母音も音程も動く）。
    // 拍に乗ってしゃべる場面（ラップ・詠唱・秒読み）は現実にいくらでもある。
    name: 'speech-steady.wav',
    note: '拍に乗って等間隔にしゃべる声',
    speech: true,
    hard: true,
    options: { noiseLevel: 0.00002, steady: true, seed: 15 },
  },
  // 2026-09-11 以前の合成の声（倍音の重みも f0 も発話中ずっと固定）は、**一覧から外した**。
  // スペクトルの形の上ではトレモロの楽器と同じもので、声の模型としては壊れている。
  // これを「守るべき声」として一覧に置くと、判定のほうを素材の欠陥に縛ることになる
  // （実際、そのせいで包絡をコマ単位の門にする手が止まっていた）。
  // 過去の数字を測り直したいときだけ makeShort に `{ flat: true }` を渡して作る。
  {
    // **揺れの割合（modulation）の分母をいじる手**を潰しにいく素材（2026-09-12・3 回目）。
    // いまの分母には「その窓に含まれる揺れ全部」が敷いてある。だから遅く大きなうねりがあると、
    // 音節帯の取り分は分母に食われて小さく出る。**遅いうねりが速い刻みを隠している**わけで、
    // 「遅い帯を分母から外す」のは筋が通って見える。
    // ところがそれをやると、**うねりに隠れていた浅い刻みが表に出てくる**。
    // ゆっくり大きくうねり（0.5Hz・深さ 0.45）、その上に声と同じ速さで浅く刻む音楽（4.2Hz・深さ 0.08）は、
    // 外したとたんに声らしさが跳ね上がるはず。うねりで音量が大きく上下するので
    // 「全編が鳴っている」に落ちず、破れれば声ゼロの素材が切り刻まれる形で実害が出る。
    name: 'music-swell.wav',
    note: 'ゆっくり大きくうねり、浅く刻む音楽（声は無い）',
    speech: false,
    hard: true,
    options: {
      speech: false,
      bgm: true,
      bgmLevel: 0.45,
      bgmSwellDepth: 0.45,
      bgmTremolo: 4.2,
      bgmTremoloDepth: 0.08,
      noiseLevel: 0.0005,
      seed: 18,
    },
  },
  {
    // **「雑音の粒があるか」系の手がかり全部**を潰しにいく素材（2026-09-13）。
    // 2026-09-12（2 回目）に「雑音の粒と音程の粒が短い間に混じるか」を測って、
    // `music-wah` はようやく音楽の側へ落ちたのに、`drums.wav` が完全に破った。
    // そのとき記録に書いた見立てが **「手元の音楽 6 本のうち 5 本が正弦波の和で、
    // 雑音を持つのは drums だけ。だから雑音を手がかりにすると全部お手盛りになる」**。
    // 雑音を持つ音楽が 1 本しか無い状態では、その見立て自体が確かめられない。
    //
    // `music-chords.wav` と**ハイハットの有無だけ**が違う（和音・背景・種はすべて同じ 14）。
    // 2 本を並べれば、動いた数字はハイハットだけが理由だと言い切れる。
    // 和音がゆっくり変わる上で高い雑音が音節の速さで刻む、というのは
    // 現実のポップスの伴奏そのもので、意地悪というより**普通の音楽**に近い。
    name: 'music-hats.wav',
    note: '和音の上でハイハットが刻む音楽（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, chordEvery: 1.5, hat: 4.2, hatLevel: 0.1, noiseLevel: 0.0005, seed: 14 },
  },
  {
    // **「低い帯域の音量が音節の速さでどれだけ深く揺れたか」を素材単位の判定に置く手**を
    // 潰しにいく素材（2026-09-16・2 回目）。
    //
    // その手は `music-hats` を止めるために考えたもので、効く理由ははっきりしている——
    // **持続する和音は、和音が変わっても音量が動かない**（低い側の揺れの深さが最大 0.32dB）。
    // 揺らしているのはハイハットで、それは 6kHz より上にしか居ないので、
    // 低い側だけを見れば `music-hats` は「何も揺れていない曲」になる。
    //
    // だとすれば、**同じ刻みを低い側へ置けば、揺れがそのまま低い側に戻ってくる**はず。
    // `music-hats.wav` の `hat` を `thump`（寄せる高さだけを変えた打点）に差し替えてある。
    // 刻みの速さも減衰も引く乱数も和音も種も同じなので、2 本の差は
    // **打点がどの帯域に居るか**だけになる。
    //
    // `speech-sparse-thump.wav`（同じ打点の上でたまにしゃべる）と対になる素材で、
    // 「声がある / 無い」だけが違う 2 本が低い側で並ぶ。現実の素材としても無理は無く、
    // キックやタムで刻む伴奏そのもの。
    name: 'music-thump.wav',
    note: '和音の上で低い打点が刻む音楽（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, chordEvery: 1.5, thump: 4.2, thumpLevel: 0.1, noiseLevel: 0.0005, seed: 14 },
  },
  {
    // **`music-thump` が「削減 0%」で済んでいる理由**を切り分けるための素材
    // （2026-09-16・2 回目）。`music-hats` に対する `music-hats-break` と同じ役目。
    //
    // `music-thump` は素材単位の判定を 3 つとも素通りしている（割合 99%・形 13.0 秒・
    // 深さ 1.89dB）。それでも削減が 0% なのは、**13 秒ずっと同じ大きさで鳴っているから**で、
    // 判定が止めたからではない。**守れているのではなく、まだ撃たれていない。**
    // 2026-09-14（3 回目）に `music-hats` で同じ読み違いをして、
    // 9/16（1 回目）にそれがそのまま実害になった。**今度は先に撃っておく。**
    //
    // **撃ってみたら、`music-hats` のようには壊れなかった。** 和音を休ませても
    // 声らしいコマの割合は 99% → 96% までしか落ちず、削減 0%・実害 0.00 秒のまま。
    // 低い側で打点が刻み続けているので、和音が休んでも**どこも「声らしい」まま**になる。
    // `music-hats` が切り刻まれたのは、割合が 37% と**半端に落ちた**からだった。
    // **判定が全面的に外れる素材は、外れているぶんだけ安全**（何も切らない）。
    // 2026-09-14 の「浅い誤認のほうが害が大きい」がここでも出た形で、
    // **この素材の実害がゼロなのは、いまも「まだ撃たれていない」ではなく「撃っても壊れない」。**
    //
    // `music-thump.wav` と**和音が休むかどうかだけ**が違う（打点も背景も種も同じ 14）。
    name: 'music-thump-break.wav',
    note: '和音が 2 回休んで低い打点だけになる音楽（声は無い）',
    speech: false,
    hard: true,
    options: {
      speech: false,
      chordEvery: 1.5,
      chordBreaks: [
        [3.2, 4.6],
        [8.0, 9.2],
      ],
      thump: 4.2,
      thumpLevel: 0.1,
      noiseLevel: 0.0005,
      seed: 14,
    },
  },
  {
    // **`tone`（音色の尖り具合 = 1 - 平坦さ）**を潰しにいく素材（2026-09-13）。
    // 声らしさは `modulation × tone` で測っているが、いま `tone` が声の側で高く出るのは
    // **音楽の側が正弦波の和だから**（鳴っているコマの平坦さ上位 10% が
    // bgm 0.074 / music-chords 0.040 に対し、声は 0.774〜0.823）。
    // 現実の管楽器も弓の擦り音も、音程と**同時に**広い帯域の雑音を出す。
    //
    // 声と違うのは共鳴（フォルマント）が動かないこと。管の長さは音程と一緒に変わるので、
    // 包絡は音程に付いてくるだけで、口のようには独立して動かない。
    // **「雑音があるか」では分けられず、「包絡が動くか」でだけ分かれる**はずの素材で、
    // どちらの手がかりが本物かを切り分けられる。
    name: 'music-flute.wav',
    note: '息の雑音を持つ管楽器（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, flute: true, fluteLevel: 0.07, noiseLevel: 0.0005, seed: 19 },
  },
  {
    // **音程の動きに賭ける手**を潰しにいく素材（2026-09-14）。
    //
    // 手元の音楽は、音程が固定（`bgm`・`music-tremolo`・`music-wah`）か、
    // 階段状に跳ぶ（`music-chords*`）か、音程が無い（`drums`・`hats`）かのどれかしかなかった。
    // **音程が滑らかに動く音楽が 1 本も無い状態**で「音程が動けば声」を測れば、
    // 必ず良い数字が出る。それは判定の出来ではなく、素材の偏りを測っているだけになる
    // （`music-flute` を足したときとまったく同じ構図）。
    //
    // 動かし方は声に合わせてある。ビブラートは `speak()` の抑揚と**同じ ±4%・1.7Hz**、
    // 音と音の間はポルタメント（0.09 秒）で渡り、音の中では 1 割下がる。
    // 声と違うのは**共鳴（フォルマント）が固定**であることだけ。
    // 「口が動いたか」を本当に測れている手ならここは弾けるはずで、
    // 音程の動きに引きずられる手はここで破れる。
    name: 'music-vibrato.wav',
    note: '音程が声と同じように動く楽器（声は無い）',
    speech: false,
    hard: true,
    options: { speech: false, vibrato: true, vibratoLevel: 0.075, noiseLevel: 0.0005, seed: 20 },
  },
  {
    // **重心の下降率で打点を弾く手**を潰しにいく素材（2026-09-13・2 回目）。
    // その手は「打点は重心が単調に下がり続ける／口の動きは向きがばらばら」という見立てで、
    // `music-hats` の下降率は 0.824、ほかの 20 本は 0.412〜0.588 と大きく開く。
    // ところが重心は**いちばん高い所にある弱い音に引きずられる**（和音にハイハットを
    // 和音の -34dB で足しただけで、素材の下降率が 0.493 → 0.749 まで動く）。
    // だとすれば、**声の上にハイハットを乗せれば声のコマまで打点に見える**はずで、
    // そうなればこの手は「ハイハットがあるか」を見ているだけになり、声を切ることになる。
    //
    // `speech.wav` と**ハイハットの有無だけ**が違う（声は 1 ビットも同じ。
    // そのために make-audio.mjs でハイハットを声のうしろへ移した）。
    // 音楽の上でしゃべるのは、この道具がいちばんよく使われる場面そのものなので、
    // 意地悪というより**外せない素材**に近い。
    name: 'speech-hats.wav',
    note: 'ハイハットが刻む上でしゃべる（声は speech.wav と同じ）',
    speech: true,
    hard: true,
    options: { hat: 4.2, hatLevel: 0.1, seed: 1 },
  },
  {
    name: 'speech-sparse-bgm.wav',
    note: 'BGM の上でたまにしゃべるだけ（20%）',
    speech: true,
    sparse: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.25, sparse: true, seed: 9 },
  },
  {
    // **重心の下降率を「素材単位」で使う手**を潰しにいく素材（2026-09-13・2 回目）。
    //
    // `speech-hats.wav`（びっしりしゃべる）では潰せなかった。声が鳴っているあいだは
    // 口の動きが重心を両向きに振るので、0.65 を超えるコマは 97% → 29% まで落ちる。
    // **潰せるのは、声がまばらで、かつ声が無い間も音が鳴り続けているとき。**
    // 声の無い 8 割が `music-hats` と同じ形（持続する和音の上で打点が減衰する）になるので、
    // 素材全体で見ると 83% が 0.65 を超える。**声が入っているのに打点の素材に見える。**
    //
    // `speech-sparse-bgm.wav` と**ハイハットの有無だけ**が違う（声も BGM も 1 ビットも同じ）。
    // 音楽の上でときどきしゃべるのは、この道具がいちばんよく使われる形そのもので、
    // しかも**素材単位の判定は外すと声を丸ごと失う**（取りこぼし 100%）。
    name: 'speech-sparse-hats.wav',
    note: 'ハイハットの刻む音楽の上でたまにしゃべるだけ（20%）',
    speech: true,
    sparse: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.25, sparse: true, hat: 4.2, hatLevel: 0.1, seed: 9 },
  },
  {
    // **「高い帯域だけが動いたコマは声に数えない」を素材単位の判定に置く手**を
    // 潰しにいく素材（2026-09-14・2 回目）。
    //
    // その手は、声のあるコマを 2〜5 割取りこぼしても構わない代わりに、
    // `music-hats` の割合を 99% → 4% まで落として 5% の線の下へ沈める。
    // 取りこぼしてよいのは、素材単位の判定が「13 秒のうち 5% 残っていればよい」ためだが、
    // **その余裕は「声がたっぷり入っていること」に乗っている**。
    //
    // だから声を薄くする。`speech-sparse-hats`（声は 2.6/13 秒）の声を
    // **母音を伸ばす声に替える**と、2 つの取りこぼしが重なる:
    //   - 声の無い 8 割は `music-hats` と同じ形（持続音の上で打点が減衰する）
    //   - 残る 2 割の声も、低い帯域が動かないので打点と同じ顔になる
    // 声が入っているのに割合が 5% を割れば、**判定そのものが働かなくなる**
    // （何も切らずに 13 秒をそのまま返す）。
    //
    // `speech-sparse-hats.wav` と**声の伸ばし方だけ**が違う（BGM もハイハットも種も同じ 9）。
    // 音楽の上でときどき、伸ばし気味にしゃべる／歌うのは現実にいくらでもある。
    name: 'speech-sparse-sustained-hats.wav',
    note: 'ハイハットの音楽の上でたまに母音を伸ばす声（20%）',
    speech: true,
    sparse: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.25, sparse: true, sustain: true, hat: 4.2, hatLevel: 0.1, seed: 9 },
  },
  {
    // **`music-hats` が 0% で済んでいる理由**を切り分けるための素材（2026-09-14・2 回目）。
    //
    // `music-hats` は声らしいコマの割合 99%・形が動いた 13.0 秒で、素材単位の判定を
    // 2 つとも素通りしている。それでも削減が 0% なのは、**13 秒間ずっと同じ大きさで
    // 鳴り続けているから**で、判定が止めたからではない。つまりここは
    // 「守れている」のではなく「まだ撃たれていない」だけ。
    //
    // 和音を 2 回休ませてハイハットだけにすると（現実の曲のブレイク）、
    // その区間だけ音の顔つきが変わる。判定が素通りしたままなら、
    // **声がゼロの素材が切り刻まれる形で実害が出る**はず。
    //
    // `music-hats.wav` と**和音が休むかどうかだけ**が違う（ハイハットも背景も種も同じ 14）。
    name: 'music-hats-break.wav',
    note: '和音が 2 回休んでハイハットだけになる音楽（声は無い）',
    speech: false,
    hard: true,
    options: {
      speech: false,
      chordEvery: 1.5,
      chordBreaks: [
        [3.2, 4.6],
        [8.0, 9.2],
      ],
      hat: 4.2,
      hatLevel: 0.1,
      noiseLevel: 0.0005,
      seed: 14,
    },
  },
  {
    // **「揺れを低い帯域だけで見る」手**を潰しにいく素材（2026-09-16）。
    //
    // その手は `speech-sparse-hats` の切れ目 5.60 秒を渡らせないために入れたもので、
    // 効いた理由ははっきりしている——**ハイハットが 6kHz より上にしか居ないから**。
    // 低い側だけを見れば、あの 2 本は `speech-sparse-bgm` と 1 ビットも違わなくなる。
    //
    // だとすれば、**同じ刻みを低い側へ置けば、乗っているものがそのまま外れる**はず。
    // ハイハットを `thumps`（寄せる高さだけを変えた打点）に差し替えてある。
    // 刻みの速さも減衰も引く乱数も同じなので、`speech-sparse-hats.wav` との差は
    // **打点がどの帯域に居るか**だけになる。
    //
    // 現実の素材としても無理は無い。タムやキックで 4 分を刻む伴奏の上でときどきしゃべる形で、
    // むしろハイハットより低い側で刻む曲のほうが多い。
    name: 'speech-sparse-thump.wav',
    note: '低い側で刻む打点の音楽の上でたまにしゃべるだけ（20%）',
    speech: true,
    sparse: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.25, sparse: true, thump: 4.2, thumpLevel: 0.1, seed: 9 },
  },
  {
    // **「揺れの深さを、その素材でいちばん深いコマを基準に読む」手**を潰しにいく素材
    // （2026-09-18・3 回目）。
    //
    // その手は「基準を素材の中から持ってくる」ための最後の候補で、
    // 声ありの素材に限ればコマ単位の判定を 3 つの数字とも上回っていた
    // （残せた 95.4% / うち声 88.0% / 落とした背景 83.3%。詳しくは `lab:probe` の段）。
    // 乗っている前提は **「その素材でいちばん深く揺れているのは声である」**。
    //
    // だとすれば、**声より深く揺れる打点**を置けばそのまま外れるはず。
    // `speech-sparse-thump.wav` と**打点の大きさだけ**が違う（声も BGM も種も同じ 9）。
    //
    // 測ってみると、**外れ方が読みと違った。壊れるのではなく、消える。**
    // 打点が大きくなると低い側が打点の合間にへこみ、`lowBandReadable` が
    // 「窓が丸ごと鳴っている」と言わなくなる。読めた秒が **12.38s → 0.04s** まで落ちて、
    // 門は誤るのではなく**自分から働かなくなる**。2026-09-17 の
    // 「門は読めるコマにしか触れない」が、別の場所でもう一度出た形。
    //
    // 現実の素材としても無理は無い。キックが前に出たミックスは珍しくなく、
    // むしろ声より低い側が大きい曲のほうが多い。
    name: 'speech-sparse-thump-loud.wav',
    note: '打点が声より深く揺れる音楽の上でたまにしゃべるだけ（20%）',
    speech: true,
    sparse: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.25, sparse: true, thump: 4.2, thumpLevel: 0.2, seed: 9 },
  },
  {
    // **「低い側の音量が下に張り付いてたまに跳ねるのは打点」という手を潰すため**の素材
    // （2026-09-16・3 回目）。あの手（`maxLowSkew`）は、声の帯域に居座る打点を
    // 音量の**向き**（歪度）で弾く。打点は鳴っていない時間のほうが長く、
    // 音節は鳴っている時間のほうが長い、という前提に乗っている。
    //
    // だとすれば、**鳴っていない時間のほうが長い声**を置けばそのまま外れるはず。
    // きっぱり区切ってしゃべる（カウント・呼びかけ・区切ったナレーション）は珍しくないので、
    // 意地悪というより、前提が現実のどこで崩れるかを測るための素材。
    //
    // `speech-sparse-bgm.wav` と**しゃべり方だけ**が違う（BGM も種も同じ 9。
    // 乱数は音節を組み終わったあとの包絡にしか効かないので、
    // 音節の並びも母音も子音も 1 ビット同じ）。
    name: 'speech-clipped-bgm.wav',
    note: 'BGM の上で、短く区切ってたまにしゃべる（20%）',
    speech: true,
    sparse: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.25, sparse: true, clipped: true, seed: 9 },
  },
  {
    name: 'speech-chord-into.wav',
    // `speech.wav` に和音を足しただけ。声は 1 ビットも同じ（`chordInto` は乱数を引かない）。
    // 2026-09-15 に入れた「発話の頭を遡って拾う」手が、いちばん損をする形を測るための素材。
    note: '和音が鳴り止んだ瞬間にしゃべり出す',
    speech: true,
    hard: true,
    options: { chordInto: true, seed: 1 },
  },
  {
    // **`skewDropped`（向きの門を素材ごとに外す手）を潰しにいく素材**（2026-09-17・3 回目）。
    //
    // その手は 9/17（2 回目）に入れたもので、根拠は「門が触れないコマの割合（下限）が
    // 5% の線より上だから、この門で素材単位に止まるのは声のある素材だけ」だった。
    // 実際 33 本では `speech-clipped-bgm`（声あり）1 本しか外れない。
    //
    // だとすれば、**下限が線より下の「声ゼロ」の素材**を置けばそのまま外れるはず。
    // 下限は「低い側が読めないコマ」＝**窓 0.64 秒の両端＋低い側がへこむところ**なので、
    // **伴奏がうねらずに鳴り続けていれば、へこみが消えて端（4.6%）だけが残る。**
    // `speech-sparse-thump.wav` の背景（BGM＋低い打点）から声を抜き、
    // うねり（`bgmSwellDepth`）を止め、打点だけが 2 秒落ちる区間を 2 つ置いた。
    //
    // 現実の素材としても無理は無い。持続するパッドやオルガンの上でキックが刻み、
    // 落ちサビでドラムだけが抜ける形で、むしろ曲としてはありふれている。
    //
    // 打点の休符（`thumpBreaks`）は `chordBreaks` の裏返し。
    // 休符の中でも乱数とフィルタは同じだけ回すので、休符を入れただけの 2 本が並べられる。
    name: 'music-thump-drop.wav',
    note: '一定の伴奏の上で低い打点が刻み、打点だけが 2 回落ちる（声は無い）',
    speech: false,
    hard: true,
    options: {
      speech: false,
      bgm: true,
      bgmLevel: 0.25,
      bgmSwellDepth: 0,
      thump: 4.2,
      thumpLevel: 0.1,
      thumpBreaks: [
        [3.0, 5.0],
        [8.0, 10.0],
      ],
      noiseLevel: 0.0005,
      seed: 9,
    },
  },
  {
    // **深さ（dB）の判定そのもの**を潰しにいく素材（2026-09-18）。
    //
    // 深さは「割合では線を引けない」を解いた量として 2026-09-16（2 回目）に既定になったが、
    // dB の深さは**その窓の中の最大と最小の比**なので、**一定の伴奏を足すと縮む**。
    // 声が動いた絶対量は同じでも、下に敷かれた伴奏が大きいほど比は 1 に近づく。
    // `speech-bgm-loud.wav` と**伴奏のうねりの有無だけ**が違う（音量も種も同じ 0.4 / 6）ので、
    // 2 本を並べれば「深さが見ているのは声の動きか、伴奏とのコントラストか」が出る。
    // うねり（0.5Hz）はそれ自体が低い側を上下させるので、**うねりが深さを底上げしていた**。
    //
    // 一定の伴奏の上でしゃべるのは、ループ物の BGM を敷いた実況・解説でごく普通の形。
    name: 'speech-flat-bgm.wav',
    note: 'うねらない伴奏が大きい上でしゃべる',
    speech: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.4, bgmSwellDepth: 0, noiseLevel: 0.0005, seed: 6 },
  },
  {
    // 上と同じ形で、伴奏をもう一段上げたもの（声より 1.6dB 大きい）。
    // **こちらは既定の深さの線（0.8dB）を割る**ので、声があるのに素材ごと止まる
    // ＝ 道具が丸ごと働かない。線をどこに置くかを決めるときの「守りたい下限」。
    name: 'speech-flat-bgm-loud.wav',
    note: 'うねらない伴奏が声より大きい上でしゃべる',
    speech: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.6, bgmSwellDepth: 0, noiseLevel: 0.0005, seed: 6 },
  },
  {
    // **低い側の線を「その素材の低い側の大きさから何 dB 下」で置く手**を潰しにいく素材
    //  （2026-09-19）。
    //
    // その手は「全域の線を低い側に当てると、低い側が大きい素材ほど読めなくなる」を直すもので、
    // 線を低い側の分布から作る。だとすれば、**素材の中で低い側の大きさが動けば外れる**はず。
    // 基準を素材ぜんたい（p90）で取ると、大きかった頃の値が小さくなった側にも残るので、
    // 後半が丸ごと「鳴っていない」になる。
    //
    // `speech-flat-bgm.wav` と**伴奏がフェードするかどうかだけ**が違う（音量も種も同じ 0.4 / 6）。
    // 13 秒で 24dB 下がる＝ナレーションの下で BGM がフェードアウトしていく、ごく普通の形。
    // 段差ではなく連続に下げてあるので、**縁（曲の切れ目）の話と混ざらない。**
    name: 'speech-bgm-fade.wav',
    note: '伴奏がフェードアウトする上でしゃべる',
    speech: true,
    hard: true,
    options: { bgm: true, bgmLevel: 0.4, bgmSwellDepth: 0, bgmFadeDb: -24, noiseLevel: 0.0005, seed: 6 },
  },
  {
    // **「その素材でいちばん深く揺れているコマは声である」をコマ単位で使う手**を
    // 潰しにいく素材（2026-09-19・2 回目）。
    //
    // その手（`minEnergyDepthDrop`）は、低い側の揺れを対数を外した深さで測り、
    // **素材の中の最大から何 dB 下か**で声と背景を分ける。基準が素材の中なので
    // 倍率には強いが、乗っている前提は 2 つある:
    // ① いちばん深く揺れているのは声、② 声はそこから 6dB 以内に居る。
    //
    // **この素材は 2 つとも同時に外しにいく。** 低い側を刻む打点が基準を奪い（①）、
    // 母音を伸ばす声は低い側をほとんど動かさない（②）。
    // `speech-sparse-thump-loud.wav` は①だけを外す素材で、そちらは門が**黙る**
    // （読めるコマが減るのではなく、線が下がりきって誰も落ちない）。
    // ①と②が重なったときに初めて、門は**黙らずに声を食う**はず。
    //
    // `speech-sustained-hats.wav` と**刻むものが高い側か低い側かだけ**が違う
    // （声も種も同じ 12。ハイハットは低い側に何も出さないので門に触れない）。
    // 伴奏が低い側で刻む上で母音を伸ばすのは、歌もの・詠唱・ゆっくりしたナレーションで普通の形。
    name: 'speech-sustained-thump.wav',
    note: '低い側で刻む打点の上で母音を長く伸ばす声',
    speech: true,
    hard: true,
    options: { noiseLevel: 0.00002, sustain: true, thump: 4.2, thumpLevel: 0.1, seed: 12 },
  },
];

/** その素材の発話の並び。 */
export function utterancesOf(fixture) {
  if (!fixture.speech) return [];
  return fixture.sparse ? SPARSE_UTTERANCES : UTTERANCES;
}

/** 時刻が発話の中かどうか。声の無い素材は常に false。 */
export function isSpeechAt(fixture, seconds) {
  return utterancesOf(fixture).some(([from, to]) => seconds >= from && seconds < to);
}
